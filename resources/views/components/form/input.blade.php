{{-- Usage

 --}}
<div class="input-group input-group-merge">
    <input type="{{ $type ?? 'text' }}" id="{{ $name }}" name="{{ $name }}" value="{{ $value ?? '' }}"
        class="form-control text-start" placeholder="{{ $placeholder ?? '' }}" >
</div>
